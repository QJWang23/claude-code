# AI-First 研发流程设计 - 实施指南

> 基于 developflow.png 传统开发流程的AI辅助变革方案

## 背景

**目标团队：** 中型后端研发团队（10-50人）
**当前工具：** Claude Code + Cursor
**核心痛点：** 代码质量不一致、交付速度慢、知识沉淀差、测试覆盖不足
**流程定位：** 全新设计AI-First流程

---

## 一、传统流程 vs AI-First流程对比

### 传统开发流程（基于 developflow.png）

```mermaid
flowchart TB
    subgraph 传统流程
        R1[需求] --> R2[设计] --> R3[开发] --> R4[测试] --> R5[发布]
        R1 -.->|"人工产出"| R2
        R2 -.->|"人工产出"| R3
        R3 -.->|"人工产出"| R4
        R4 -.->|"人工产出"| R5
    end
    
    style R1 fill:#e1f5fe
    style R2 fill:#e1f5fe
    style R3 fill:#e1f5fe
    style R4 fill:#e1f5fe
    style R5 fill:#e1f5fe
```

**输出件清单：**
| 阶段 | 输出件 |
|------|--------|
| 需求 | PRD/需求规格说明书 |
| 设计 | 架构设计文档、接口定义、技术方案 |
| 开发 | 源代码、单元测试用例 |
| 测试 | 测试报告、缺陷清单 |
| 发布 | 部署包、监控告警、运维手册 |

---

### AI-First 研发新流程

```mermaid
flowchart TB
    subgraph 需求阶段
        R1[用户/PM需求] --> R2[AI解析生成PRD]
        R2 --> R3[人工审核确认]
        R3 --> R4[需求基线]
    end
    
    subgraph 设计阶段
        R4 --> D1[AI生成架构方案]
        D1 --> D2[人工评审调整]
        D2 --> D3[设计基线]
    end
    
    subgraph 开发阶段
        D3 --> C1[AI编码实现]
        C1 --> C2[AI自动测试]
        C2 --> C3[人工Code Review]
        C3 --> C4[代码入库]
    end
    
    subgraph 测试阶段
        C4 --> T1[AI生成测试用例]
        T1 --> T2[AI执行测试]
        T2 --> T3[人工验收]
        T3 --> T4[测试通过]
    end
    
    subgraph 发布阶段
        T4 --> P1[AI自动部署]
        P1 --> P2[AI监控]
        P2 --> P3[异常处理]
        P3 --> P4[上线完成]
    end
    
    subgraph 新增:AI质量关卡
        Q1[AI Gate Check] -.->|不通过| C3
        Q1 -.->|通过| P1
    end
    
    style R1 fill:#e1f5fe
    style R2 fill:#90caf9
    style R3 fill:#fff3e0
    style R4 fill:#c8e6c9
    style D1 fill:#90caf9
    style D2 fill:#fff3e0
    style D3 fill:#c8e6c9
    style C1 fill:#90caf9
    style C2 fill:#90caf9
    style C3 fill:#fff3e0
    style C4 fill:#c8e6c9
    style T1 fill:#90caf9
    style T2 fill:#90caf9
    style T3 fill:#fff3e0
    style T4 fill:#c8e6c9
    style P1 fill:#90caf9
    style P2 fill:#90caf9
    style P3 fill:#fff3e0
    style P4 fill:#c8e6c9
    style Q1 fill:#f3e5f5
```

**图例说明：**
- 蓝色节点：AI主导环节（80%产出）
- 橙色节点：人工审核环节（20%决策）
- 绿色节点：基线/交付物
- 紫色节点：新增质量关卡

---

## 二、各阶段详细流程

### 阶段一：需求阶段

```mermaid
flowchart LR
    subgraph INPUT[输入]
        A1[自然语言需求]
        A2[用户故事]
        A3[原始PRD]
    end
    
    subgraph AI_PROCESS[AI处理 80%]
        B1[需求解析]
        B2[结构化提取]
        B3[验收标准生成]
        B4[歧义识别]
    end
    
    subgraph HUMAN_PROCESS[人工审核 20%]
        C1[业务逻辑确认]
        C2[边界标注]
        C3[需求批准]
    end
    
    subgraph OUTPUT[输出]
        D1[标准化PRD]
        D2[验收用例库]
        D3[需求基线]
    end
    
    A1 & A2 & A3 --> B1
    B1 --> B2 --> B3 --> B4
    B4 --> C1 --> C2 --> C3
    C3 --> D1 & D2 & D3
```

**输入输出控制：**

| 类型 | 内容 |
|------|------|
| 输入 | 自然语言需求、用户故事、原始PRD |
| AI输出 | 标准化PRD文档（格式统一、边界清晰、验收标准完整） |
| 人工输出 | 需求审核意见、修正说明 |
| 最终输出 | 需求基线PRD（带版本号） |

**AI/人分工：**
| 角色 | 职责 |
|------|------|
| AI (80%) | 解析模糊需求→结构化PRD、提取验收标准、生成验收用例、识别潜在歧义 |
| 人工 (20%) | 审核业务逻辑正确性、确认验收标准、批准需求基线 |

**关键控制点：**
- ✅ 需求验收标准必须人工确认
- ✅ 业务边界必须人工标注
- ✅ 需求变更必须触发AI重新生成

---

### 阶段二：设计阶段

```mermaid
flowchart TB
    subgraph 架构设计
        A1[需求基线] --> A2[AI生成架构方案]
        A2 --> A3[技术选型建议]
        A3 --> A4[人工评审]
        A4 --> A5[架构决策]
    end
    
    subgraph 详细设计
        A5 --> B1[AI生成接口定义]
        B1 --> B2[AI生成数据模型]
        B2 --> B3[AI生成流程图]
        B3 --> B4[人工评审]
        B4 --> B5[设计基线]
    end
    
    subgraph 新增:AI设计检查
        A2 -.->|自动检查| Q1[架构合理性检查]
        B1 -.->|自动检查| Q2[接口一致性检查]
        Q1 & Q2 --> Q3[设计质量报告]
    end
```

**设计阶段AI/人分工：**

| 环节 | AI职责 | 人工职责 |
|------|--------|----------|
| 架构方案生成 | 生成多套架构选型、对比分析 | 评审业务适配性、做最终决策 |
| 接口定义 | 生成OpenAPI/Proto定义 | 确认业务语义正确性 |
| 数据模型 | 生成ER图、DDL脚本 | 确认业务边界和约束 |
| 流程图 | 生成时序图、泳道图 | 确认业务流程正确性 |

---

### 阶段三：开发阶段

```mermaid
flowchart TB
    subgraph 开发准备
        A1[设计基线] --> A2[AI理解代码库]
        A2 --> A3[AI生成实现计划]
    end
    
    subgraph AI编码
        A3 --> B1[AI生成代码]
        B1 --> B2[AI生成单元测试]
        B2 --> B3[AI自测验证]
    end
    
    subgraph 代码评审
        B3 --> C1[人工Code Review]
        C1 -->|有修改| B1
        C1 -->|通过| C2[代码入库]
    end
    
    subgraph 新增:AI质量关卡
        B1 -.->|静态检查| Q1[AI Lint Check]
        B2 -.->|覆盖率| Q2[AI覆盖率检查]
        C2 -.->|门禁| Q3[AI Gate Check]
    end
```

**AI编码规范：**

```mermaid
graph TD
    subgraph CLAUDE.md必含内容
        C1[项目架构说明]
        C2[代码规范摘要]
        C3[技术栈约束]
        C4[质量要求标准]
    end
    
    subgraph AI编码约束
        P1[必须遵循的规则]
        P2[禁止的模式]
        P3[命名约定]
    end
    
    C1 & C2 & C3 & C4 --> P1
    P1 --> P2 --> P3
```

**关键配置：** 项目根目录必须有 `CLAUDE.md` 文件，包含项目架构、代码规范、技术约束。

---

### 阶段四：测试阶段

```mermaid
flowchart LR
    subgraph 测试输入
        T1[代码变更]
        T2[设计文档]
        T3[需求基线]
    end
    
    subgraph AI测试生成
        T1 & T2 & T3 --> A1[AI分析测试点]
        A1 --> A2[AI生成单元测试]
        A2 --> A3[AI生成集成测试]
        A3 --> A4[AI生成端到端测试]
    end
    
    subgraph AI测试执行
        A4 --> E1[AI执行测试]
        E1 --> E2[AI分析失败]
        E2 -->|有失败| A2
        E2 -->|通过| E3[测试报告]
    end
    
    subgraph 人工验收
        E3 --> H1[人工验收确认]
        H1 --> H2[测试通过]
    end
```

**测试覆盖增强：**

| 测试类型 | AI能力 | 人工职责 |
|----------|--------|----------|
| 单元测试 | 自动生成，覆盖率>80% | 审核边界用例 |
| 集成测试 | 自动生成接口测试 | 确认业务场景 |
| 端到端测试 | 自动生成用户旅程 | 验收用户体验 |
| 回归测试 | 智能选择回归范围 | 确认关键路径 |

---

### 阶段五：发布阶段

```mermaid
flowchart TB
    subgraph 发布准备
        P1[测试通过] --> P2[AI生成部署清单]
        P2 --> P3[AI检查配置]
    end
    
    subgraph 部署执行
        P3 --> D1[AI执行部署]
        D1 --> D2[AI验证服务]
        D2 --> D3[AI配置监控]
    end
    
    subgraph 监控运维
        D3 --> M1[AI日志监控]
        M1 --> M2[AI异常检测]
        M2 -->|正常| M3[运行中]
        M2 -->|异常| M4[AI生成告警]
        M4 --> H1[人工处理]
        H1 --> M1
    end
```

---

## 三、新增核心机制

### 1. AI上下文管理（CLAUDE.md体系）

```mermaid
flowchart TB
    subgraph 项目级上下文
        P1[PROJECT.md] --> P2[CLAUDE.md]
    end
    
    subgraph 模块级上下文
        M1[模块1/CLAUDE.md]
        M2[模块2/CLAUDE.md]
    end
    
    subgraph 任务级上下文
        T1[TASK.md]
    end
    
    P2 --> M1 & M2
    M1 & M2 --> T1
```

| 文件 | 作用 | 维护者 |
|------|------|--------|
| `CLAUDE.md` | 项目级AI上下文，包含架构、规范、约束 | 团队维护 |
| `模块/CLAUDE.md` | 模块级AI上下文 | 模块owner |
| `TASK.md` | 任务级AI上下文 | 任务执行时生成 |

**CLAUDE.md模板：**

```markdown
# 项目CLAUDE.md

## 项目概述
- 项目名称、目标、范围
- 核心业务逻辑说明

## 技术架构
- 技术栈及选型理由
- 架构设计图
- 模块划分说明

## 代码规范
- 命名约定
- 目录结构约定
- 注释规范

## 质量要求
- 测试覆盖率要求 (>80%)
- 代码评审要求
- 提交信息规范

## 禁止事项
- ❌ 不允许的模式
- ❌ 安全敏感操作
```

---

### 2. AI质量关卡（AI Gate Check）

```mermaid
flowchart TB
    subgraph Quality_Gates[质量关卡]
        G1[编码规范检查] --> G2[安全扫描]
        G2 --> G3[测试覆盖率检查]
        G3 --> G4[代码复杂度检查]
        G4 --> G5[技术债检查]
    end
    
    G1 -.->|不通过| R1[阻塞发布]
    G2 -.->|不通过| R1
    G3 -.->|不通过| R1
    G4 -.->|不通过| R1
    G5 -.->|不通过| R1
    
    G5 -.->|通过| P1[允许发布]
```

**质量关卡检查项：**

| 检查项 | 阈值 | 处理方式 |
|--------|------|----------|
| 代码规范 | 100%通过 | 阻塞 |
| 安全扫描 | 0高危漏洞 | 阻塞 |
| 覆盖率 | >80% | 警告/阻塞 |
| 复杂度 | Cyclomatic<15 | 警告 |
| 技术债 | <5%新增 | 警告 |

---

### 3. 能力沉淀机制

```mermaid
flowchart LR
    subgraph 沉淀内容
        S1[Skill模板库]
        S2[Prompt模板库]
        S3[代码模板库]
        S4[最佳实践库]
    end
    
    subgraph 复用方式
        R1[新项目接入]
        R2[新人培训]
        R3[团队推广]
    end
    
    S1 & S2 & S3 & S4 --> R1 & R2 & R3
```

**推荐沉淀的Skills：**

| Skill名称 | 触发场景 | 内容要点 |
|-----------|----------|----------|
| `code-review` | 代码提交后 | 团队代码规范、常见问题检查清单 |
| `test-generation` | 开发完成后 | 测试策略、覆盖率要求、用例模板 |
| `api-design` | 接口设计时 | RESTful规范、命名约定、版本管理 |
| `db-migration` | 数据库变更时 | 变更规范、回滚策略 |
| `deploy-check` | 发布前检查 | 发布清单、回滚预案 |
| `incident-response` | 故障处理时 | 故障分级、响应流程 |

---

## 四、AI/人职责划分

```mermaid
graph TB
    subgraph AI核心能力[AI核心能力 - 70%产出]
        A1[需求解析与结构化]
        A2[代码生成与重构]
        A3[测试用例生成]
        A4[文档自动生成]
        A5[代码评审辅助]
        A6[异常检测与分析]
    end
    
    subgraph 人工核心能力[人工核心能力 - 30%决策]
        H1[业务逻辑判断]
        H2[技术架构决策]
        H3[代码审核批准]
        H4[用户体验验收]
        H5[异常处理决策]
        H6[团队协作沟通]
    end
    
    subgraph 协作边界[协作边界]
        B1[AI不越界: 业务决策]
        B2[AI不越界: 伦理判断]
        B3[AI不越界: 人员管理]
        B4[人不越界: 重复编码]
        B5[人不越界: 机械测试]
        B6[人不越界: 文档搬运]
    end
    
    A1 & A2 & A3 & A4 & A5 & A6 --> B1 & B2 & B3 & B4 & B5 & B6
    B1 & B2 & B3 & B4 & B5 & B6 --> H1 & H2 & H3 & H4 & H5 & H6
```

**职责边界定义：**

| AI可以做 | AI不可做 |
|----------|----------|
| 解析需求生成PRD | 决定需求优先级 |
| 生成架构方案 | 选择最终架构 |
| 编写代码实现 | 批准代码入库 |
| 生成测试用例 | 验收用户体验 |
| 执行自动化部署 | 处理生产故障 |
| 分析监控数据 | 决定告警策略 |

---

## 五、团队推广方案

```mermaid
flowchart LR
    subgraph 第一阶段[阶段一:试点验证 2周]
        P1[选择1个试点项目]
        P2[配置CLAUDE.md]
        P3[沉淀核心Skills]
        P4[验证流程可行性]
    end
    
    subgraph 第二阶段[阶段二:小范围推广 4周]
        S1[扩展到3个项目]
        S2[完善能力沉淀]
        S3[培训3-5名种子用户]
        S4[收集反馈迭代]
    end
    
    subgraph 第三阶段[阶段三:全员推广 8周]
        F1[覆盖所有项目]
        F2[建立能力中台]
        F3[全员培训]
        F4[持续优化机制]
    end
    
    P1 --> P2 --> P3 --> P4
    P4 --> S1 --> S2 --> S3 --> S4
    S4 --> F1 --> F2 --> F3 --> F4
```

**推广里程碑：**

| 阶段 | 时间 | 目标 | 验收标准 |
|------|------|------|----------|
| 试点验证 | 第1-2周 | 验证流程可行性 | 完成需求到发布全流程 |
| 小范围推广 | 第3-6周 | 完善能力沉淀 | 研发效率提升20% |
| 全员推广 | 第7-14周 | 团队全面覆盖 | 研发效率提升50% |

---

## 六、效果评估指标

| 指标类型 | 指标名称 | 基准值 | 目标值 |
|----------|----------|--------|--------|
| 效率 | 需求到交付周期 | 14天 | 7天 |
| 效率 | 代码编写效率 | 基准 | +100% |
| 质量 | 代码缺陷率 | 5% | 3% |
| 质量 | 测试覆盖率 | 60% | 80% |
| 质量 | 线上故障率 | 基准 | -50% |
| 能力 | Skills复用率 | 0% | 70% |
| 能力 | 新人上手时间 | 30天 | 15天 |

---

## 七、实施前置条件清单

- [ ] Claude Code 已安装并配置
- [ ] Cursor 已安装（可选）
- [ ] 代码仓库已准备
- [ ] CLAUDE.md规范已制定
- [ ] 试点项目已选定
- [ ] 种子用户已确定

---

## 八、下一步行动

1. **确认方案** - 与团队确认此AI-First流程设计
2. **准备环境** - 配置Claude Code、创建CLAUDE.md
3. **试点启动** - 选择试点项目开始验证
4. **持续迭代** - 根据实践反馈优化流程

---

*文档版本: v1.0*
*创建日期: 2026-04-14*
*基于: developflow.png 传统开发流程*